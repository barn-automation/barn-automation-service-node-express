/**
* A set of path route rules.
* @param {Array<PathRoute>} pathRoutes The set of path route rules.
* @class PathRouteSetDetails
*/
class PathRouteSetDetails {
	constructor(pathRoutes){
		this.pathRoutes = pathRoutes;
	}
}

module.exports = PathRouteSetDetails;
