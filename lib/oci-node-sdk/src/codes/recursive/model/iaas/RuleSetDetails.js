/**
* The rules that compose a rule set.
* @param {Array<Rule>} items An array of rules that compose the rule set.
* @class RuleSetDetails
*/
class RuleSetDetails {
	constructor(items){
		this.items = items;
	}
}

module.exports = RuleSetDetails;
